/target
/book/book
/.env
.env.local
*.log
/logs
.claude/
.idea/
.vscode/
*.iml

# Internal operator handoff notes — never public. Do not commit.
# The file exists in dev history up to 2026-09-06 and needs to be
# scrubbed from the tree going forward. History rewrite would be a
# separate, coordinated operation.
HANDOFF.md

# Local drafts for downstream release announcements (Slack, mailing
# list, GitHub Release notes body, etc.). The published version
# should go through `gh release edit` or the target channel, not the
# repo tree — these are working files only.
announcement-v*.md
